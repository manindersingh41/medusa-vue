/**
 * Augment the typings of Vue.js
 */

import Vue from "vue";
import { VueLazyloadHandler } from "vue-lazyload";

declare module "vue/types/vue" {
  interface Vue {
    $Lazyload: VueLazyloadHandler;
  }
}

